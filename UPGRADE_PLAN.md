# Self-Improving Agent → 5/5: план доработки

Цель: довести проект из «профессионально структурированного фасада» (исполнительное ядро не подключено) до настоящего прод-агента уровня Claude Code/Hermes, без компромиссов. База — 5 аудитов 2026-06-15.

Текущие оценки → цель:
| Дименсия | Сейчас | Цель |
|---|---|---|
| Агентный цикл | 2/5 | 5/5 |
| Tool-слой | 2/5 | 5/5 |
| Память | 3/5 | 5/5 |
| Самоулучшение | 3/5 | 5/5 |
| Прод-готовность/честность | 2/5 | 5/5 |

Линчпин: **Фаза 1** (проводка агентного цикла). Без неё остальные фазы бессмысленны — это 80% ценности.

---

## Фаза 0 — Единый источник правды (полдня)
Незакоммиченная работа размазана: `/var/www/agent` (правки memory/clients/config, не в git), `/var/www/new` = GitHub `b970a65`. Сначала свести в одну ветку.
- [ ] Снять с US-сервера незакоммиченную дельту `/var/www/agent` (memory/manager,retriever,store,embeddings,config, clients/*), отревьюить, влить в git или отбросить осознанно.
- [ ] Зафиксировать рабочую ветку `feat/agentic-core`, дальше всё в неё.
- [ ] Прогнать существующие 3 sanity-теста как baseline.

## Фаза 1 — Агентный цикл (P0, ядро) → 2→5
Сейчас `MainAgent.process()` (`agents/main_agent.py:220-245`) — один стрим без `tools`; `ToolRegistry` в рантайме не инстанцируется.
- [ ] **P0** Инстанцировать `ToolRegistry` в точке входа (`interfaces/cli.py`, `main.py`) с регистрацией дефолтных инструментов; пробросить в `MainAgent`.
- [ ] **P0** Реализовать настоящий loop в `MainAgent.process()`: `client.chat_with_tools(tools=registry.get_anthropic_tools())` → парс `tool_use` → `registry.execute` → вернуть `tool_result` → повтор, пока модель не закончит.
- [ ] **P0** Ограниченный цикл: `max_iterations` (конфиг), per-tool timeout, детектор зацикливания (повтор одинаковых tool_call). Убрать `while True` без потолка в `analyzer.py:232`, `versioner.py:208`.
- [ ] **P0** Ошибка-как-tool_result: обернуть `_execute_tool` (try/except), при ошибке инструмента вернуть текст ошибки моделью как `tool_result`, не рвать цикл (`versioner.py:244`, `analyzer.py:295`).
- [ ] **P1** OAuth-фолбэк + ретраи в `chat_with_tools` (зеркало `chat()`, `anthropic_client.py:213` сейчас без `_should_fallback`).
- [ ] **P1** Реальный учёт токенов из `message_delta.usage` вместо `len(text)//4` (`main_agent.py:229`); кормить `cost_tracker`.
- [ ] **P1** Стриминг с tool-use (стрим текста + детект tool_use).
- [ ] **P1** Субагенты с инструментами и контекст-циклом (сейчас `sub_agent.py:38-55` — single-shot, ошибки глотаются в строку как «успех»).
- Acceptance: агент реально читает/правит файлы/гитует/гуглит в одном диалоге; на 5 реальных задачах (фикс бага, рефактор, добавить тест, исследовать репо, мульти-файл правка) проходит без зависаний и крашей; ошибки инструментов восстанавливаются.

## Фаза 2 — Tool-слой: безопасность и полнота (P0) → 2→5
- [ ] **P0** Подключить `approval/` к `registry.execute`: CONFIRM-инструменты (`write_file`/`run_command`/`git_commit`) реально дёргают `Confirmator` (сейчас `registry.py:165-174` — passthrough).
- [ ] **P0** Подключить `UndoManager`: `record_file_write` на каждой записи (`filesystem.py`), иначе откат — пустышка.
- [ ] **P0** Закрыть shell-инъекцию: `shell.py:177` `create_subprocess_shell` + обход блок-листа через `;`/`&&`/`||`/`|`/`>`. Перейти на argv-exec без shell или жёстко блокировать операторы (не warning); `bash_security` должен ловить цепочки с разрешённой головной командой.
- [ ] **P0** Добавить `EditFileTool` (old_string→new_string) + `MultiEdit` (сейчас только запись файла целиком; комментарии «(future) FileEditTool» в `file_state.py:23`).
- [ ] **P1** Атомарная запись (temp+rename) в `filesystem.py:150`, `notebook.py:81`.
- [ ] **P1** Stale-detection: сравнение mtime/hash диска перед записью (`file_state.was_modified_since_read` сейчас смотрит только свои записи).
- [ ] **P1** SSRF-guard в `web_fetch.py:101` (блок localhost/127/169.254.169.254/private); offset/limit и лимит размера в `ReadFileTool` (`filesystem.py:61`); реальный glob (`**`, сортировка по mtime).
- [ ] **P2** HTML-парсер вместо регэкспов (`web_fetch`/`web_search`); git push/branch/log как инструменты; убрать дубль DANGEROUS_GIT_PATTERNS (`shell.py:17` vs `permissions.py:38`).
- Acceptance: ни одна мутация без подтверждения/возможности отката; shell не исполняет цепочку через разрешённую голову; правки точечные и атомарные; внешние правки файла не затираются молча.

## Фаза 3 — Память: подключить RLM, не плодить вторую (P0/P1) → 3→5
Своя память (4 типа + RRF) крепкая, но позади RLM-схемы claude-memory (нет ФС-навигации кодом, нет инстинктов, brute-force вектор, нет шедулера). Не доводить её до 5/5 с нуля — **переиспользовать claude-memory как backend**.
- [ ] **P0** Подключить claude-memory (RLM) как память агента через его MCP-клиент (`mcp/` уже есть) ИЛИ как библиотеку: `memory_search/grep/read/save/instinct_*` в loop.
- [ ] **P1** Перенести в claude-memory два гема этого репо (полезны обоим): `memory/secret_scanner.py` (скан секретов ПЕРЕД записью — критично с учётом облака z.ai) и `memory/bounds.py` (программный лимит размера индекса — решает распухший MEMORY.md).
- [ ] **P1** Периодическая консолидация/дистилляция в собственном цикле агента (аналог нашего fact-дистиллятора каждые N ходов; сейчас `consolidate()` без шедулера).
- [ ] **P2** Если оставлять локальный store — заменить brute-force вектор (`retriever.py:97`) на sqlite-vec/FAISS.
- [ ] **P1** Pinning важных сообщений в `compactor.py` + обратная подгрузка из session_memory (сейчас обрезка до 2000 симв теряет данные).
- Acceptance: агент помнит профиль/проекты между сессиями через RLM; инстинкты применяются; секреты не попадают в память; индекс не пухнет неограниченно.

## Фаза 4 — Самоулучшение: замкнуть контур (P0/P1) → 3→5
Петля работает end-to-end, но разомкнута: нет защиты от деградации, метрики мертвы, детектор ловит «исправь/fix this» как негатив о себе.
- [ ] **P0** Замкнуть контур качества: писать метрики версий (`sessions_count`/pos/neg rate — сейчас `prompts.py:111-114` инициализируются и не обновляются), canary/A-B новой версии, авто-откат при просадке ниже baseline (сейчас новая версия активируется безусловно, `prompts.py:134`).
- [ ] **P0** Починить FeedbackDetector (`core/feedback.py:51-70`): рабочие команды кодеру («исправь баг в X», «fix this function») НЕ считать негативным фидбеком о себе; требовать явный мета-сигнал или LLM-классификацию высокой точности; убрать захардкоженный confidence 0.85.
- [ ] **P1** Защита мета-уровня: versioner не может переписывать промпт analyzer/versioner/собственный (`versioner.py:90` enum); реальный `validate_prompt` (сохранены ли security-инструкции, нет ли инверсии правил/инъекции — сейчас только длина/формат).
- [ ] **P1** Убрать гонку глобального конфига в `ManualImprovementOrchestrator.run` (`orchestrator.py:234-241`) при фоновых `asyncio.create_task`.
- [ ] **P2** Недостижимая ветка `category=="explicit"` (`feedback.py:32`); пустой `evidence` (`analyzer.py:368`); turn_id обнуляется при рестарте (`logs.py:46`).
- Acceptance: вредная версия промпта авто-откатывается; рабочие команды не триггерят дрейф промпта; agent не может испортить свой мета-мозг без отката.

## Фаза 5 — Провайдеры: схема fcm как backend (P1) → укрепляет 1 и прод
Решение по fcm: НЕ оборачивать агент в CCR (агент уже делает нативную Anthropic↔OpenAI конверсию в `clients/base.py` — CCR = двойная трансляция + лишние отказы). Вместо этого:
- [ ] **P1** Добавить `fcm` как первоклассный backend в `clients/factory.py`: один OpenAI-совместимый эндпоинт на FCM-роутер (free-coding-models) → бесплатный пул с health-probe и авто-failover, который мы уже отладили.
- [ ] **P1** Перенести приёмы sanitize-слоя в `clients/base.py`/клиенты: срезать неподдерживаемые провайдером поля (reasoning/cache_control), капить max_tokens, нативный SSE-pipe (без буферизации), per-provider нормализация tool-формата.
- [ ] **P1** Курированный ростер free-моделей (умная первой — zai-glm-4.7 — для агентного цикла, быстрый фолбэк вторым) как пресет; OAuth/нативные провайдеры — «премиум»-маршрут для качества.
- [ ] **P2** `check_api_keys()` в `main.py` не знает про openrouter — починить (дефолт на openrouter проходит проверку без ключа и падает позже).
- Acceptance: агент работает на бесплатном пуле с авто-failover из коробки; апгрейд на платный провайдер/подписку — переключением; tool-calling стабилен на всех провайдерах.

## Фаза 6 — Прод-готовность и честность (P0/P1) → 2→5
- [ ] **P0** Тесты: покрыть агентный цикл, исполнение инструментов, approval/undo, память, самоулучшение, MCP-adapter. Цель — осмысленное покрытие, не 3 sanity-теста на 15.6k строк.
- [ ] **P0** Честность README: после Фаз 1-5 заявки становятся правдой; до тех пор — привести README в соответствие с реальностью (никаких обещаний нерабочего).
- [ ] **P1** Активировать `plugins/loader.load_all()` на старте (сейчас не вызывается, `/plugins` всегда пуст); провести MCP-инструменты в инференс (сейчас только CLI connect/list, в loop не попадают); voice-бинари (sox/whisper) — в optional extras + документировать.
- [ ] **P1** CI (GitHub Actions): тесты + линт + проверка ID моделей провайдеров.
- [ ] **P2** Verification-agent сделать tool-enabled (сейчас «проверяет» не запуская).
- Acceptance: `pytest` зелёный с реальным покрытием; README не врёт ни в одном пункте; чистый старт без скрытых блокеров.

---

## Порядок и проверка
1. Фазы строго по порядку: 0 → 1 → 2 → (3,4,5 можно частично параллелить) → 6.
2. После каждой фазы — **цикл аудит→фикс→re-audit до 0 находок** (как в остальных проектах), отдельным агентом на дименсию.
3. Финал: повторный полный 5-аудит, цель — 5/5 по всем дименсиям + честный README + зелёный CI.

## Грубая оценка объёма
- Фаза 1 (ядро) — самая большая, основной риск и ценность.
- Фазы 2,4,6 — средние.
- Фазы 3,5 — переиспользуют уже готовое (claude-memory, fcm), относительно дёшевы.
- Критично: это «достроить агентный харнесс уровня Claude Code». Реалистично — большой проект, но фундамент (абстракции, мультипровайдер, версионирование) уже заложен качественно.
