# Self-Improving AI Agent

Самоулучшающийся AI-агент, который эволюционирует через обратную связь. Вдохновлён [winning solution ERC3](https://erc.timetoact-group.at/assets/erc3.html) - системой, где агенты улучшают агентов.

## Ключевая идея

В отличие от обычных чат-ботов, где "память" сжимается со временем, этот агент **перманентно улучшает свой системный промпт** на основе вашего фидбека. Каждое улучшение сохраняется навсегда.

> 💡 **Бесплатные модели**: Агент поддерживает множество бесплатных моделей через Groq и OpenRouter. Платный API Anthropic нужен только для pipeline улучшений.

```
Вы: "Слишком длинный ответ"
     ↓
[Analyzer] анализирует логи, формулирует гипотезы
     ↓
[Versioner] генерирует улучшенный промпт
     ↓
Новая версия промпта сохраняется (v1 → v2 → v3...)
     ↓
Следующие ответы уже с новым "мозгом"
```

## Архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                         Пользователь                            │
└─────────────────────────────────────────────────────────────────┘
                                │ диалог
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                         MAIN AGENT                              │
│   Llama 3.3/4 / Qwen 3 / DeepSeek R1 / GLM 4.7 / Claude         │
│    Разговорный интерфейс, выполнение задач, детекция фидбека    │
└───────────┬─────────────────────────────────────┬───────────────┘
            │ при фидбеке                         │ логирует
            ▼                                     ▼
┌───────────────────────┐                 ┌───────────────────────┐
│    ANALYZER AGENT     │◄────────────────│   data/logs/*.jsonl   │
│  Claude Sonnet        │                 └───────────────────────┘
│  анализ, гипотезы     │
└───────────┬───────────┘
            │ гипотезы
            ▼
┌───────────────────────┐
│   VERSIONER AGENT     │
│  Claude Sonnet        │
│  генерация промптов   │
└───────────┬───────────┘
            │ новый промпт
            ▼
┌───────────────────────┐
│ data/prompts/v*.yaml  │
│   версионирование     │
└───────────────────────┘
```

### Три агента

| Агент | Модель | Задача |
|-------|--------|--------|
| **Main Agent** | Любая (Llama 3.3 70B по умолчанию) | Общение с пользователем |
| **Analyzer** | Claude Sonnet (требует tool_use) | Анализ логов, формулирование гипотез |
| **Versioner** | Claude Sonnet (требует tool_use) | Генерация улучшенных промптов |

## Быстрый старт

### Вариант 1: Docker (рекомендуется)

```bash
# Клонировать репозиторий
git clone https://github.com/YOUR_USERNAME/self-improving-agent.git
cd self-improving-agent

# Настроить ключи
cp .env.example .env
nano .env  # Добавить GROQ_API_KEY

# Запустить
make run
# или
docker compose run --rm agent
```

### Вариант 2: Локальная установка

```bash
git clone https://github.com/YOUR_USERNAME/self-improving-agent.git
cd self-improving-agent
python -m venv venv
source venv/bin/activate
pip install -e .
cp .env.example .env
nano .env  # Добавить API ключи
agent      # Запустить
```

### Конфигурация API ключей

#### Вариант A: Полностью бесплатно 🆓

```bash
# Groq - рекомендуется (быстрый, стабильный)
GROQ_API_KEY=gsk_...          # https://console.groq.com/
DEFAULT_MODEL=llama-3.3-70b

# Или OpenRouter (больше моделей, но rate limits)
# OPENROUTER_API_KEY=sk-or-... # https://openrouter.ai/keys
# DEFAULT_MODEL=deepseek-r1
```

> ⚠️ Без ANTHROPIC_API_KEY pipeline улучшений не работает, но диалоги работают.

#### Вариант B: Полный функционал 💰

```bash
# Groq для Main Agent (бесплатно)
GROQ_API_KEY=gsk_...
DEFAULT_MODEL=llama-3.3-70b

# Anthropic для pipeline улучшений
ANTHROPIC_API_KEY=sk-ant-...
```

### Запуск

```bash
# Docker (рекомендуется)
make run

# Или локально
agent
```

## Использование

### Базовый диалог

Просто пишите сообщения - агент отвечает с использованием текущего системного промпта.

```
You: Напиши функцию сортировки на Python
Assistant: [ответ]
```

### Обратная связь

Когда что-то не нравится - скажите об этом:

```
You: Слишком длинный ответ, можно короче?
Assistant: [короткий ответ]
---
_Feedback detected. Starting improvement analysis..._
```

Агент автоматически:
1. Детектирует фидбек
2. Анализирует что пошло не так
3. Обновляет свой промпт
4. Следующие ответы будут учитывать ваш фидбек

### Явный фидбек

```
/feedback Отвечай всегда на русском языке
```

### CLI команды

| Команда | Описание |
|---------|----------|
| `/help` | Показать все команды |
| `/model` | Показать текущую модель и доступные |
| `/model <name>` | Переключить модель |
| `/prompt` | Показать текущий системный промпт |
| `/versions` | История версий промпта |
| `/rollback N` | Откатить к версии N |
| `/feedback TEXT` | Отправить явный фидбек |
| `/stats` | Статистика сессии |
| `/history` | История диалога |
| `/status` | Статус pipeline улучшения |
| `/reset` | Сбросить диалог |
| `/clear` | Очистить экран |
| `/quit` | Выход |

## Модели

### 🆓 Бесплатные модели

#### Groq (FREE) — Сверхбыстрый inference ⚡

| Модель | ID | Описание |
|--------|-----|----------|
| **Llama 3.3 70B** | `llama-3.3-70b` | Лучшая универсальная, по умолчанию |
| **Llama 4 Maverick** | `llama-4-maverick` | Новейшая Llama 4 (128 experts) |
| Llama 4 Scout | `llama-4-scout` | Llama 4 компактная (16 experts) |
| Qwen 3 32B | `qwen3-32b` | Thinking mode, показывает рассуждения |
| Kimi K2 | `kimi-k2` | Moonshot AI |
| GPT-OSS 120B | `gpt-oss-120b` | OpenAI Open Source |

> Регистрация: https://console.groq.com/ (14,400 запросов/день бесплатно)

#### OpenRouter (FREE) — Агрегатор моделей

| Модель | ID | Описание |
|--------|-----|----------|
| **DeepSeek R1** | `deepseek-r1` | Лучший reasoning, Chain-of-Thought |
| Llama 3.3 70B | `llama-3.3-70b-or` | Meta Llama через OpenRouter |
| Llama 3.1 405B | `llama-3.1-405b` | Самая большая бесплатная! |
| Qwen 3 Coder | `qwen3-coder` | Для кодинга |
| Gemma 3 27B | `gemma-3-27b` | Google Gemma 3 |
| Mistral Small | `mistral-small` | Mistral AI 24B |

> Регистрация: https://openrouter.ai/keys
> ⚠️ Free tier имеет rate limits, рекомендуем Groq как основной

#### Zhipu AI (FREE tier) — Китайские модели

| Модель | ID | Описание |
|--------|-----|----------|
| **GLM-4.7** | `glm-4.7` | Новейшая версия |
| GLM-4.5 Air | `glm-4.5-air` | Баланс скорость/качество |
| GLM-4.5 Flash | `glm-4.5-flash` | Быстрая |
| GLM-4 Plus | `glm-4-plus` | Мощная |

> Регистрация: https://open.bigmodel.cn/ или https://z.ai/
> ⚠️ Free tier имеет строгий rate limit

### 💰 Платные модели

#### Anthropic (Claude)

| Модель | ID | Описание |
|--------|-----|----------|
| **Claude Opus 4.5** | `claude-opus-4.5` | Лучшая модель Anthropic |
| Claude Sonnet 4 | `claude-sonnet` | Баланс скорость/качество |
| Claude Opus 4 | `claude-opus` | Предыдущая версия Opus |
| Claude Haiku 3.5 | `claude-haiku` | Быстрая и дешёвая |

> ⚠️ **Pipeline улучшений** (Analyzer + Versioner) требует Anthropic API для tool_use

### Переключение модели

В CLI:
```
/model llama-3.3-70b     # Groq, быстрая, по умолчанию
/model llama-4-maverick  # Groq, новейшая Llama 4
/model qwen3-32b         # Groq, с thinking mode
/model deepseek-r1       # OpenRouter, reasoning
/model claude-opus-4.5   # Anthropic, лучшая (платная)
```

Или в `.env`:
```bash
DEFAULT_MODEL=llama-3.3-70b   # Groq (рекомендуется)
DEFAULT_MODEL=deepseek-r1     # OpenRouter
DEFAULT_MODEL=claude-opus-4.5 # Anthropic (платная)
```

## Конфигурация

### Переменные окружения (.env)

```bash
# ========= API КЛЮЧИ =========

# БЕСПЛАТНЫЕ провайдеры (рекомендуется хотя бы один)
GROQ_API_KEY=gsk_...              # Groq - очень быстрый
OPENROUTER_API_KEY=sk-or-...      # OpenRouter - много моделей

# ПЛАТНЫЕ провайдеры
ANTHROPIC_API_KEY=sk-ant-...      # Для Claude и pipeline улучшений
ZHIPU_API_KEY=...                 # Для GLM моделей

# ========= МОДЕЛИ =========

# Основной агент (любая модель)
DEFAULT_MODEL=llama-3.3-70b        # Groq, рекомендуется

# Pipeline улучшения (только Anthropic - требует tool_use)
ANALYZER_MODEL=claude-sonnet
VERSIONER_MODEL=claude-sonnet
FEEDBACK_MODEL=claude-haiku

# ========= ПОРОГИ =========

FEEDBACK_CONFIDENCE_THRESHOLD=0.8
IMPROVEMENT_CONFIDENCE_THRESHOLD=0.6

# ========= ЛОГИРОВАНИЕ =========

LOG_LEVEL=INFO
```

### Провайдеры и функционал

| Провайдер | Main Agent | Analyzer | Versioner | Цена |
|-----------|:----------:|:--------:|:---------:|------|
| **OpenRouter** | ✅ | ❌ | ❌ | FREE |
| **Groq** | ✅ | ❌ | ❌ | FREE |
| **Zhipu AI** | ✅ | ❌ | ❌ | FREE tier |
| **Anthropic** | ✅ | ✅ | ✅ | Платно |

### Режимы работы

**Полностью бесплатный режим** (без улучшений):
- Только `GROQ_API_KEY` или `OPENROUTER_API_KEY`
- Main Agent работает
- Pipeline улучшений отключён

**Полный режим** (с улучшениями):
- `ANTHROPIC_API_KEY` + любой бесплатный провайдер
- Main Agent на бесплатной модели
- Pipeline улучшений через Claude

## Структура проекта

```
/var/www/agent/
├── pyproject.toml                 # Зависимости Python
├── Dockerfile                     # Docker образ
├── docker-compose.yml             # Docker Compose конфигурация
├── Makefile                       # Команды (make run, make build, etc.)
├── install.sh                     # Скрипт автоустановки
├── .env                           # Конфигурация (создать из .env.example)
├── .env.example                   # Шаблон конфигурации
├── README.md                      # Эта документация
│
├── src/agent/
│   ├── main.py                    # Точка входа CLI
│   ├── config.py                  # Конфигурация
│   │
│   ├── agents/
│   │   ├── base.py                # Базовый класс агента
│   │   ├── main_agent.py          # Основной разговорный агент
│   │   ├── analyzer.py            # Агент анализа логов
│   │   └── versioner.py           # Агент генерации промптов
│   │
│   ├── clients/
│   │   ├── base.py                # Абстракция LLM клиента
│   │   ├── anthropic_client.py    # Claude (платный)
│   │   ├── groq_client.py         # Groq (бесплатный)
│   │   ├── openrouter_client.py   # OpenRouter (бесплатный)
│   │   ├── zhipu_client.py        # Zhipu/GLM (free tier)
│   │   └── factory.py             # Фабрика клиентов
│   │
│   ├── core/
│   │   ├── feedback.py            # Детекция фидбека
│   │   └── orchestrator.py        # Координация улучшений
│   │
│   ├── storage/
│   │   ├── prompts.py             # Версионирование промптов
│   │   └── logs.py                # Логирование диалогов
│   │
│   └── interfaces/
│       └── cli.py                 # CLI интерфейс
│
├── data/
│   ├── prompts/
│   │   ├── main_agent/
│   │   │   ├── v001_initial.yaml  # Начальный промпт
│   │   │   ├── v002_*.yaml        # Улучшенные версии
│   │   │   └── current.yaml       # Симлинк на активную версию
│   │   ├── analyzer/
│   │   │   └── current.yaml
│   │   └── versioner/
│   │       └── current.yaml
│   │
│   └── logs/
│       ├── conversations/         # Логи диалогов (JSONL)
│       │   └── 2026-01-29.jsonl
│       └── improvements/          # Логи улучшений
│           └── 2026-01-29.jsonl
│
└── tests/
```

## Форматы данных

### Версия промпта (YAML)

```yaml
version: 2
created_at: "2026-01-29T14:22:15Z"
parent_version: 1

improvement:
  trigger: "user_feedback"
  feedback_summary: "Ответы слишком длинные"
  hypothesis_ids: ["H1"]
  analyzer_confidence: 0.85

changes:
  - section: "## Formatting"
    change_type: "modify"
    description: "Добавлено ограничение на длину ответов"
    hypothesis_id: "H1"

system_prompt: |
  You are a helpful AI assistant...
  [полный текст промпта]

metrics:
  sessions_count: 15
  positive_feedback_rate: 0.8
  negative_feedback_rate: 0.1

metadata:
  author: "versioner_agent"
  approved: false
```

### Лог диалога (JSONL)

```json
{
  "timestamp": "2026-01-29T14:22:15.123Z",
  "session_id": "sess_abc123",
  "turn_id": 1,
  "type": "turn",
  "user_message": "Напиши функцию сортировки",
  "assistant_response": "Вот функция...",
  "prompt_version": 2,
  "model": "claude-opus-4-5-20251101",
  "tokens": {"input": 150, "output": 420},
  "latency_ms": 1250,
  "feedback": null
}
```

### Лог с фидбеком

```json
{
  "timestamp": "2026-01-29T14:23:45.456Z",
  "session_id": "sess_abc123",
  "turn_id": 2,
  "user_message": "Слишком длинно",
  "assistant_response": "Понял...",
  "feedback": {
    "type": "negative",
    "category": "verbosity",
    "raw_text": "Слишком длинно",
    "confidence": 0.92,
    "triggered_improvement": true
  }
}
```

## Детекция фидбека

Агент автоматически распознаёт фидбек по паттернам:

### Негативный фидбек (триггерит улучшение)

- "слишком длинно/коротко/сложно"
- "не понял", "не то", "неправильно"
- "можно короче/проще/понятнее"
- "переделай", "исправь"

### Позитивный фидбек (логируется)

- "спасибо", "отлично", "супер"
- "помогло", "работает", "то что нужно"

### Категории

| Категория | Примеры |
|-----------|---------|
| `verbosity` | длинно, коротко, многословно |
| `accuracy` | неправильно, ошибка, неверно |
| `clarity` | непонятно, сложно, запутано |
| `format` | формат, структура, код |
| `tone` | грубо, формально, неформально |

## Pipeline улучшения

### 1. Детекция

`FeedbackDetector` анализирует каждое сообщение:
- Паттерн-матчинг (быстро)
- LLM fallback для неясных случаев (Haiku)

### 2. Анализ (Analyzer Agent)

Инструменты:
- `search_logs` - поиск по логам
- `get_conversation` - получить диалог
- `get_prompt_history` - история промптов
- `submit_analysis` - отправить результат

Выход:
- Список проблем с severity
- Гипотезы улучшений с confidence
- Примеры из логов

### 3. Версионирование (Versioner Agent)

Инструменты:
- `get_current_prompt` - текущий промпт
- `validate_prompt` - валидация
- `create_prompt_version` - создание версии

Принципы:
- Инкрементальные изменения
- Связь с гипотезами
- Документирование каждого изменения

### 4. Активация

Новая версия сохраняется в `data/prompts/main_agent/v00N_*.yaml` и становится активной через симлинк `current.yaml`.

## Откат версий

Если улучшение ухудшило поведение:

```
/versions          # посмотреть историю
/rollback 1        # вернуться к версии 1
```

## Разработка

### Установка для разработки

```bash
pip install -e ".[dev]"
```

### Запуск тестов

```bash
pytest
```

### Линтинг

```bash
ruff check src/
ruff format src/
```

## Расширение

### Добавление нового провайдера

1. Создайте клиент в `src/agent/clients/`:

```python
from .base import BaseLLMClient, LLMResponse

class MyClient(BaseLLMClient):
    provider = "myprovider"

    # Список поддерживаемых моделей
    MODELS = {
        "my-model": "provider/actual-model-id",
        "my-alias": "provider/my-model",
    }

    @classmethod
    def list_models(cls) -> list[str]:
        return list(cls.MODELS.keys())

    def chat(self, messages, system=None, max_tokens=4096, tools=None):
        # Реализация синхронного API вызова
        pass

    def stream(self, messages, system=None, max_tokens=4096):
        # Реализация стриминга (async generator)
        pass
```

2. Добавьте маппинг в `factory.py`:

```python
# В MODEL_PROVIDERS
MODEL_PROVIDERS["my-model"] = "myprovider"
MODEL_PROVIDERS["my-alias"] = "myprovider"

# В create_client()
elif provider == "myprovider":
    from .my_client import MyClient
    return MyClient(api_key=..., model=model)

# В get_available_models() и get_free_models()
```

3. Добавьте в `__init__.py`:

```python
from .my_client import MyClient
```

### Добавление инструментов агентам

Инструменты описываются в формате Anthropic tool_use:

```python
TOOLS = [
    {
        "name": "my_tool",
        "description": "Description",
        "input_schema": {
            "type": "object",
            "properties": {...},
            "required": [...]
        }
    }
]
```

## Docker

### Команды

```bash
make build    # Собрать образ
make run      # Запустить агента (интерактивно)
make stop     # Остановить
make logs     # Логи
make shell    # Shell в контейнере
make clean    # Удалить всё
```

### Docker Compose

```bash
# Запуск
docker compose run --rm agent

# Фоновый режим (для HTTP API в будущем)
docker compose up -d

# Остановка
docker compose down
```

### Переменные окружения

Все настройки через `.env` или переменные:

```bash
docker run -it --rm \
  -e GROQ_API_KEY=gsk_xxx \
  -e DEFAULT_MODEL=llama-3.3-70b \
  -v $(pwd)/data:/app/data \
  self-improving-agent
```

## Статус провайдеров (январь 2026)

| Провайдер | Статус | Лучшие модели | Примечание |
|-----------|--------|---------------|------------|
| **Groq** | ✅ Работает | `llama-3.3-70b`, `llama-4-maverick`, `qwen3-32b` | Рекомендуется, 14,400 req/day |
| **OpenRouter** | ⚠️ Rate limits | `deepseek-r1`, `llama-3.1-405b` | Free tier ограничен |
| **Zhipu AI** | ⚠️ Rate limits | `glm-4.7`, `glm-4.5-air` | Нужен баланс |
| **Anthropic** | 💰 Платный | `claude-opus-4.5`, `claude-sonnet` | Для pipeline улучшений |

### Рекомендуемая конфигурация

```bash
# Минимальная (бесплатно)
GROQ_API_KEY=gsk_...
DEFAULT_MODEL=llama-3.3-70b

# Полная (с улучшениями)
GROQ_API_KEY=gsk_...
ANTHROPIC_API_KEY=sk-ant-...
DEFAULT_MODEL=llama-3.3-70b
```

## Лицензия

MIT

## Вдохновение

- [ERC3 Winning Solution](https://erc.timetoact-group.at/assets/erc3.html) - self-evolving agents
- [Anthropic Multi-Agent Systems](https://www.anthropic.com/engineering/multi-agent-research-system)
- [Self-Evolving Agents Cookbook](https://cookbook.openai.com/examples/partners/self_evolving_agents/autonomous_agent_retraining)
